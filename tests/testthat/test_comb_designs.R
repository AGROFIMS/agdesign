library(dplyr)
library(stringr)
library(magrittr)
library(testthat)
library(agdesign)
context("test get labels for residue management")

test_that("Test fertilizer tab for numbers of fertilizer", {
  
  
  fname <- rprojroot::find_testthat_root_file("userInput", "table_ids.rds")
  allinputs <- readRDS(fname)
  
  
  
  
  
  
  
  
})